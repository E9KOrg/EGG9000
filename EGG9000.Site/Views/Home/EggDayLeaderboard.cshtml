@using static EGG9000.Site.Controllers.HomeController;
@model List<EggDayResults>
@inject Discord.WebSocket.DiscordSocketClient discord
@{
	ViewData["Title"] = "Leaderboard";

	var claimsIdentity = (System.Security.Claims.ClaimsIdentity)User.Identity;
	var discordId = claimsIdentity.Claims.First(x => x.Type == "DiscordId").Value;

	var myAccounts = Model.Where(x => x.UserAccount.User.DiscordId.ToString() == discordId);
	var isAdmin = User.IsInRole("Admin") || User.IsInRole("GuildAdmin");

	//var guild = discord.GetGuild(Model.First().UserAccount.User.GuildId);
}


@if (myAccounts.Count() == 1) {
	<a class="btn btn-secondary" href="#@myAccounts.First().UserAccount.Account.Id">Jump to me</a>
} else if (myAccounts.Count() > 1) {
	foreach (var account in myAccounts) {
		<a class="btn btn-secondary" href="#@myAccounts.First().UserAccount.Account.Id">Jump to @myAccounts.First().UserAccount.Account.Name</a>
	}
}
<style>
	th {
		position: sticky;
		top: 0;
		text-align: center;
	}

		th img, td img {
			height: 20px;
		}

	table {
		width: auto !important;
	}



	.Me td {
		background-color: #adffaa !important;
	}

	.bootstrap-dark .Me td {
		background-color: #2c462b !important;
	}

	.monospace {
		font-size: 0;
		white-space: nowrap;
	}

		.monospace span {
			width: 0.53em;
			font-size: 1rem;
			display: inline-block;
			text-align: center;
		}

			.monospace span.period {
				width: auto;
			}
</style>
<div>
Sort by
<a href="?sortby=eb&prefix=@(ViewBag.prefix)"><button class="btn btn-primary">EB Gain</button></a>
<a href="?sortby=se&prefix=@(ViewBag.prefix)"><button class="btn btn-primary">SE Gain</button></a>
<a href="?sortby=seper&prefix=@(ViewBag.prefix)"><button class="btn btn-primary">% Gain</button></a>
<a href="?sortby=prestige&prefix=@(ViewBag.prefix)"><button class="btn btn-primary">Prestiges</button></a>
</div>
<div>
	Filter by
@foreach(var prefix in SIPrefix.Prefixes) {
    <a href="?sortby=@(ViewBag.sortby)&prefix=@prefix.Base"><button class="btn btn-primary">@prefix.Name</button></a>
}
</div>
<div class="row">
	<div class="col">
		<table class="table table-striped table-sm">
			<thead>
				<tr>
					<th></th>
					<th style="text-align: left;">Name</th>
@*					<th><img src="~/images/Egg_soul.png" alt="Soul Eggs" /></th>
					<th><img src="~/images/Egg_of_Prophecy.png" alt="Eggs of Prophecy" /></th>
					<th>EB</th>
*@					<th>EB Gain</th>
					<th>SE Gain</th>
					<th>% Gain</th>
					<th>Prestiges</th>
				</tr>
			</thead>
			<tbody>
				@{
					var i = 0;
					foreach (var account in Model.Where(x => x.EBGain > 0 && x.PrestigeCount > 0)) {
						i++;
						RowTemplate(account, i);
					}
				}
			</tbody>
		</table>

	</div>
</div>

@{
	void MonoSpace(string str) {
		foreach (var c in str) {
			<span class="@(c == '.' ? "period" : "")">@c</span>
		}
	}
	void RowTemplate(EggDayResults account, int i) {
		var isMe = account.UserAccount.User.DiscordId.ToString() == discordId;
		
		var ebrgx = new Regex(@"\(.+?\)");
		var name = ebrgx.Replace(account.UserAccount.User.DiscordUsername, "").Trim();


		<tr id="@account.UserAccount.Account.Id" class="@(isMe ? "Me" : "")">

			<td align="right"> @i </td>

			<td style="max-width: 150px; text-overflow: ellipsis; line-break: strict;">
				@if (isMe) {
					<div id="@account.UserAccount.Account.Id" style="position: relative; top: -40px;"></div>
				}
				@name
			</td>
			<td>
				@account.EBGain.ToEggString()
			</td>
			<td>
				@account.SEGain.ToEggString()
			</td>
			<td>
				@account.SEGainPercent.ToString("P0")
			</td>
			<td>@account.PrestigeCount</td>
		</tr>
	}
}
